// Define uma interface chamada IAvaliavel com um método avaliar.
export interface IAvaliavel {

    // Método que deve ser implementado pelas classes que implementam esta interface.
    avaliar(): void;
}
